# NOTE APP ON CLI
quick and in-hand for CLI user and ADHD.

## Installation:
 - change NOTE_DIR in source code.
 - run `cargo build --release`.
 - add to PATH.

## Instruction:
```bash
note {new/find} {content}
```
